<?xml version="1.0" encoding="utf-8"?>
<!-- Launch splash, Android 11 and below. Keep this in sync with
     drawable-v21/launch_background.xml — v21+ shadows this file, so editing only
     one of the two leaves every modern phone on the old plain background. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/launch_background" />

    <item>
        <bitmap
            android:gravity="center"
            android:src="@mipmap/ic_launcher" />
    </item>
</layer-list>
