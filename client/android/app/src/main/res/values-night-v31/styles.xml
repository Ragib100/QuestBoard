<?xml version="1.0" encoding="utf-8"?>
<!-- Deliberately identical to values-v31. QuestBoard has no dark theme
     (decisions.md D2), so the splash must stay light even when the OS is in dark
     mode — otherwise the app flashes a dark splash and then opens white. -->
<resources>
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowSplashScreenBackground">@color/launch_background</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
    </style>
</resources>
