# Runtime
fastapi>=0.139,<1.0
uvicorn[standard]>=0.49,<1.0
pydantic-settings>=2.14,<3.0
SQLAlchemy>=2.0,<3.0
psycopg[binary]>=3.3,<4.0
supabase>=2.31,<3.0
httpx>=0.28,<1.0        # Codeforces API + AI hints — arrives via supabase too, pinned here because we import it directly

# Optional: only needed if you set ANTHROPIC_API_KEY. The free OpenAI-compatible
# providers (Gemini, Groq, OpenRouter) go through httpx and need nothing extra,
# so `anthropic` is imported lazily and this line can be dropped.
anthropic>=0.122,<1.0

# Tooling — run `ruff check app` and `black app` before every PR
black>=25,<26
ruff>=0.14,<1.0
pytest>=8,<9         # tests/ — needs a reachable DATABASE_URL, see tests/conftest.py
